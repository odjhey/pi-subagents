# SK-05 dogfood evidence

## Invocation

Run from `/Users/odz/packages/pi-subagents` with a temporary ignored project definition named `kernel-proof` (`context: fresh`; tools `read`, `grep`, `find`, `ls`):

```bash
touch /tmp/spawn-kernel-dogfood-start
pi --no-extensions -e "$PWD/index.ts" \
  --no-builtin-tools --tools subagent --no-session --mode json -p \
  'You must call the subagent tool exactly once. Use this exact request: {"agent":"kernel-proof","task":"Inspect src/extension/index.ts and report its production imports that belong to removal cards SK-06 policy/prompt mutation systems or SK-07 orchestration, async, integrations, and UI products. Include concrete file paths, make no edits, and state whether the subagent tool is available inside the child."}. After the tool returns, briefly relay its result without any additional tool call.' \
  > /tmp/spawn-kernel-dogfood.json \
  2> /tmp/spawn-kernel-dogfood.stderr
```

The command exited `0`. The JSON event transcript was `/tmp/spawn-kernel-dogfood.json` (51,877 bytes); stderr was empty. No credential or secret is recorded here.

## Result

Transcript parsing found exactly one `subagent` tool-execution start and one end. The normalized child result was:

- status: `completed`
- output: 420 UTF-8 bytes (64 KiB cap)
- error: absent (0 bytes; 16 KiB message cap)
- usage: `inputTokens: 16187`, `outputTokens: 946`, `cacheReadTokens: 17920`, `cacheWriteTokens: 0`, `costUsd: 0.118275`

The child reported that `src/extension/index.ts` has no direct production imports belonging to either SK-06 policy/prompt mutation systems or SK-07 orchestration/async/integration/UI systems. That is useful confirmation that SK-04 moved the production entry path off those legacy areas. It reported no edits and explicitly reported that `subagent` was unavailable inside the child.

## Side-effect check and cleanup

```bash
find .pi .subagents -type f -newer /tmp/spawn-kernel-dogfood-start -print 2>/dev/null | sort || true
git status --short
```

The `find` command printed no files: the fresh launch created no repository background, artifact, mission, or session files. `git status --short` was unchanged by the run. The temporary `.pi/agents/kernel-proof.md` definition was removed after verification and is absent from the final diff.
